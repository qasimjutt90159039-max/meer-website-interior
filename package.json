{
  "name": "meers-interior-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Meer's Interior - Office Furniture Store Full-Stack Website",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node copy-dist.js",
    "postinstall": "npm --prefix frontend install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.12.1"
  }
}
